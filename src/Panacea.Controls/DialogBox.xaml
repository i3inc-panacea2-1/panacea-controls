<local:DialogBaseWindow x:Class="Panacea.Controls.DialogBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Panacea.Controls"
        mc:Ignorable="d"
                        x:Name="Main">
    <Grid DataContext="{Binding ElementName=Main}">
        <Border BorderBrush="LightGray" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Margin="10" Text="{Binding Text}">
                </TextBlock>
                <Button HorizontalAlignment="Center" Grid.Row="1" Click="Button_Click">Close</Button>
            </Grid>
        </Border>
    </Grid>
</local:DialogBaseWindow>
